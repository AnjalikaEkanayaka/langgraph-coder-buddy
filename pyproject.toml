[project]
name = "langgraph-coder-buddy"
version = "0.1.0"
description = "Beginner-friendly LangGraph multi-agent code generator"
requires-python = ">=3.10"
dependencies = [
  "langgraph>=0.2.0",
  "langchain>=0.2.0",
  "langchain-groq>=0.1.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
